{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Daymark main window permissions",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
