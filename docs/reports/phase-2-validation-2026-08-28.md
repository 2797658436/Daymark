# Daymark 阶段 2 检测记录（2026-08-28）

- 检测日期：2026-08-28（Asia/Shanghai）
- 候选提交：`0534bbc92cc33edf1cc55bb2e005aa6fb9c19be6`
- 桌面程序：`src-tauri/target/release/daymark.exe`
- 检测方法：全套自动化结果核对 + Windows 桌面界面实际操作
- 依据方案：[`phase-2-21-day-validation-plan.md`](phase-2-21-day-validation-plan.md)

## 结论

**本次即时技术与桌面冒烟检测通过，未发现 S0 数据安全阻断、S1 核心流程阻断或 S2 重要缺陷。**

这不是阶段 2 的最终 21 天产品验证结论。连续使用是否降低选择压力、开始阻力和错过后的恢复成本，仍需按验证方案完成 21 天记录后判断。

## 自动化与构建

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 前端生产构建 | 通过 | TypeScript `--noEmit` 与 Vite production bundle 成功 |
| Vitest | 通过 | 7 个测试文件，55 个测试通过 |
| Playwright | 通过 | 9 个端到端测试通过 |
| Rust | 通过 | 24 个测试通过 |
| Tauri / NSIS release | 通过 | release 编译成功并生成 `Daymark_0.1.0_x64-setup.exe` |

自动化结果来自同日、同一提交和同一工作区的 `npm run check`。GUI 检测期间只产生应用数据，没有修改产品代码。

首次打包时 GUI 检测进程仍占用 `src-tauri/target/release/daymark.exe`，导致构建工具无法替换该文件。结束已保存完成的检测进程后重新执行，release 与 NSIS 打包正常完成；这属于测试环境文件占用，不是产品编译缺陷。

## 桌面检测结果

### T00 启动、导航与持久化基线：通过

- release 桌面程序正常启动并显示 Daymark 窗口。
- 成功读取已有项目、任务、历史安排、默认时段、主题和备份列表。
- 今日、日历、项目、7 天回顾、数据、设置六个页面均可进入。
- 页面切换时显示“正在保存”，完成后恢复“已保存到本机”。
- 完成 release/NSIS 构建后启动新进程，检测任务、`2026-08-29 19:00` 安排、每日习惯、今日 `22:07–22:37` 发生项和“开始／结束记录未开启”设置均跨进程保留。

### T01 快速创建与自动排程 Lite：通过（撤销仅由自动化覆盖）

实际操作：

1. 快速创建任务 `【阶段2检测】快速创建任务`。
2. “未安排”筛选中只显示一份该任务，没有重复副本。
3. 自动排程初始展示已有三个活动任务和新任务，容量为未来 7 天默认时段空档。
4. 取消选择三个既有任务后，草案只包含检测任务。
5. 预览结果为 `2026-08-29 19:00–20:00`，60 分钟。
6. 应用后任务卡显示下一次安排为 `2026-08-29 19:00`，日历无既有安排被移动的迹象。

未通过 GUI 执行“撤销本次自动排程”，因为该操作会删除刚写入的执行时段；整批撤销和原子回滚已由现有 Vitest、Rust 与 Playwright 测试覆盖。

### T02 轻量时间块：通过（冲突排除由自动化覆盖）

- 创建时间块 `【阶段2检测】时间块`，日期为 `2026-08-28`，时间为 `12:00–13:00`。
- 日历中时间块和任务执行时段以不同卡片呈现。
- 时间块没有进度，也没有进入任务池。
- 7 天回顾仍保持安静空状态，说明计划时间和时间块没有被伪造成实际投入或推进成果。

本次没有删除时间块，也没有再创建一个与未来自动排程冲突的时间块；删除和排程避让由自动化测试覆盖。

### T03 基础重复习惯：部分通过

- 创建每天重复的 `【阶段2检测】每日习惯`，单次投入 30 分钟。
- 创建后只在“重复习惯”区域出现一个定义，没有在普通任务池复制出普通任务。
- 点击“安排今天”后，发生项状态变为“已安排”，日历出现对应习惯时段。
- 今日页能够显示该习惯的独立安排状态。

未在本次即时检测中覆盖工作日、每周选日、固定开始时间、跳过发生项和跨日期不累积；这些路径已有自动化覆盖，仍建议在 21 天使用中遇到并记录。

### T04 错过后的挽救提示：通过一个真实动作

实际操作：

1. 临时开启“记录开始／结束本次”，确认开始宽限期控件随之启用；挽救提示原本已开启。
2. 今日页对已经过期的习惯时段显示一次中性挽救卡。
3. 卡片准确显示“现在开始”“延后 10 分钟”“重新选择时间”“本次跳过”四个动作。
4. 执行“延后 10 分钟”后，时段从原定 `19:00–19:30` 调整为 `22:07–22:37`，没有自动开始，也没有改变 0% 完成度。
5. 挽救卡消失，今日时间线显示新的一个执行时段。
6. 检测结束后已把“记录开始／结束本次”恢复为原来的关闭状态；今日页重新显示“开始／结束记录未开启”。

其他三个动作未在同一发生项上重复执行；其行为由现有自动化测试覆盖。

### T05 最近 7 天回顾：通过空状态

- 页面窗口为 `2026-08-22` 至 `2026-08-28`，恰好覆盖今天及之前六天。
- 当前只有计划时段、时间块和习惯安排，没有真实执行或手动进度事件。
- 页面因此显示安静空状态，没有全零图表，没有把未记录解释为失败。

成果、实际投入、进度增加和待续汇总需要在形成真实执行事实后继续验证。

### T06 B 站链接导入 Beta：成功预览，通过

- 输入公开视频链接 `https://www.bilibili.com/video/BV1GJ411x7h7`。
- 原生读取成功，项目标题预填为 `【官方 MV】Never Gonna Give You Up - Rick Astley`。
- 预览显示 `BV1GJ411x7h7`、一个分 P 和约 4 分钟媒体时长，并允许选择和编辑。
- 点击取消后返回项目页，没有创建新项目；现有 `Python` 项目仍为 0 个任务。

本次没有测试网络失败后的保留与重试，也没有确认写入项目；相关事务与失败路径已有自动化覆盖。

### T07 数据与备份界面：只读检查通过

- 数据页正常显示 SQLite 本地备份能力和“立即备份／导出／恢复”入口。
- 可见当天备份 `daymark-2026-08-28.db`，以及 2026-08-10、08-02、08-01、07-30、07-29 的每日备份。
- 页面没有显示自动备份失败警示。

本次没有点击“立即备份”，因为它会覆盖当天备份内容；没有执行恢复，因为恢复会替换当前数据库。恢复、回滚和偏好协调由 Rust 测试覆盖，真实 GUI 恢复应在独立环境并经用户确认后执行。

## 严重度汇总

| 等级 | 数量 | 说明 |
| --- | ---: | --- |
| S0 | 0 | 未发现数据丢失、错误覆盖或虚假保存 |
| S1 | 0 | 未发现启动、排程、习惯、挽救、回顾或导入核心阻断 |
| S2 | 0 | 未发现会系统性影响连续使用的重要缺陷 |
| S3 | 0 | 本次没有确认可稳定复现的一般缺陷 |

## 本次写入的检测数据

为避免误认为真实计划，本次对象统一使用前缀 `【阶段2检测】`：

- 任务：`【阶段2检测】快速创建任务`
- 任务执行时段：`2026-08-29 19:00–20:00`
- 时间块：`【阶段2检测】时间块`，`2026-08-28 12:00–13:00`
- 重复习惯：`【阶段2检测】每日习惯`，每天 30 分钟
- 今日习惯发生项：已安排，并经挽救动作调整为 `2026-08-28 22:07–22:37`
- 挽救提示已显示事实：已记录

这些数据没有被自动删除。删除任务、时间块、习惯或执行时段属于本地数据删除，应在用户明确确认后执行。

## 尚未完成的验证

以下事项不能由一次即时检测替代：

- 21 天中至少 15 个活动日及三次周检查点。
- 相比 Day 0，选择下一步、开始和恢复三项困难是否改善。
- 最后 7 天维护负担中位数是否不高于 3。
- 工作日／每周选日习惯的真实连续使用。
- B 站失败重试的实时网络场景。
- 独立环境中的备份预览、恢复和失败回滚 GUI 路径。
- 关闭到托盘、托盘恢复和应用内“显式退出”的完整桌面路径。

## 当前决策

阶段 2 可以继续作为 21 天候选版使用；本次没有发现需要先阻断试用的缺陷。最终是否完成阶段 2 产品验证，应在 21 天记录结束后再决定。
