# Domain documentation

Daymark is a single-context repository.

Before exploring or changing product behavior, read:

- `CONTEXT.md` at the repository root for the canonical domain language.
- Relevant architectural decisions under `docs/adr/`.

Use the glossary's canonical terms in specifications, issue titles, tests, UI copy, and implementation. Do not silently override an ADR; call out the conflict before changing the decision.
